
'use client';
import Reveal from "../Reveal";

export default function ContactSection(){
  return(
    <section id="contact" className="section-bg contact-gradient min-h-screen flex items-center justify-center">
      <Reveal>
        <button className="floating-box reveal bg-white text-black">
          Contacts (●'◡'●)
        </button>
      </Reveal>
    </section>
  )
}
